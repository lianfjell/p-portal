<%= stylesheet_link_tag "articles.css" %>

<div class="container">
  <div class="row">
    <div class="col-lg-3">
    </div>
    <div class="col-lg-6">
      <div class="card search-card">
        <div class="card-body">
          <div class="card-title">
            <h5>Søk etter artikler</h5>
          </div>
            <!-- Search form -->
            <div class="md-form mt-0">
              <%= form_tag articles_path, :method => :get do %>
              <%= text_field_tag :search, params[:search], placeholder: "Søk blant #{Article.count} artikler", class: "form-control", id: 'searchfield'%>
            </div>
            <!---->
            <a href="#" class="card-link">Avansert søk</a>
          </div>
        </div>



      <% @articles.each do |article| %>
        <div class="card article-container">
          <div class="card-body">
            <h5 class="card-title"><%= article.title%></h5>
            <p class="card-text">Some quick example text to build on the card title and make up the bulk of the card's content.</p>
            <%= link_to 'Les mer', article, class: 'btn btn-primary'%>
            <%= link_to 'Rediger', edit_article_path(article) %></td>
            <%= link_to 'Slett', article, method: :delete, data: { confirm: 'Er du sikker på at du ønsker å slette?' } %>
        </div>
       </div>
      <%end%>
      <%end%>
    </div>
    <div class="col-lg-3">
    </div>
  </div>
</div>
